# us-stock-scanner 專案進度存檔

存檔時間:2026-08-06
Repo:github.com/kkyyooaa-ghb/us-stock-scanner(public)
Notion:美股掃描 DB `37c323c3fc0180d3a84acdea1a5ca2af`

## 本機與 GitHub 同步

- 本機工作目錄先前不是 git repo(只有檔案),逐檔雜湊比對後確認內容與
  `origin/main` (`ad3a05d`) 完全相同,僅換行符差異;缺 `AGENTS.md`、
  `CLAUDE.md` 兩檔。
- 已就地 `git init` + 接 `origin` + `reset` 至 `origin/main`(未使用
  `reset --hard`,無任何本機檔案被覆蓋),還原缺檔後
  `main...origin/main` 為 0 ahead / 0 behind。
- `core.autocrlf` 設為 `input`,避免日後 checkout 把全 repo 轉成 CRLF。

## 接手時的生產狀態

- 盤前掃描跑到 2026-08-05,watchdog 全綠,無失敗 run。
- Notion 最新 2026-08-05 共 10 筆;08-01、08-02 為週末,無缺跑。
- `.scratch/` 四個既有 effort 全部 `resolved`,無在途工作。
- V1.3 調參閘門為 `collecting 2/60`(目標 100),分腿 segment 皆未達 20,
  因此所有權重／門檻調整仍被禁止。

## V1.3 P0:LLM enrichment 美股化

`llm_enrichment.py` 自台股版移植後從未改市場,是 repo 內唯一明確標為 P0
且不受校準閘門阻擋的項目。

### 病灶

- Tavily query 硬加「台股」,並保留 `.TW/.TWO` 後綴清理。
- `include_domains` 全為台灣財經媒體(cnyes、moneydj、udn、ettoday 等),
  對 NDX 成分股近乎零命中。
- Prompt 自稱「台股研究助手」,風險/催化語彙是台股用語。
- 因此 `LLM_ENRICHMENT_ENABLED` 自 V1.3 起設為 `False`,整段 P7.5 是死碼。

### 本次修正

- query 改 `公司英文名 (TICKER) stock news`;新增 `Config.COMPANY_NAMES`
  對應 `SCAN_POOL` 99 檔。純 ticker 在美股歧義過高(APP、EA、ARM、MU、STX
  同時是常用英文字),公司名可大幅收斂;查無名稱 fallback 純 ticker 不拋錯。
- `include_domains` 改 15 個美國來源:主流財經媒體、新聞稿線
  (businesswire／prnewswire／globenewswire)與 `sec.gov`。後兩類是財報、
  財測、大單與 8-K 的第一手原文,正對應 prompt 要抓的 risk/catalyst。
- Prompt 改美股研究助手:風險改財測下修、SEC 調查、內部人賣股、大客戶流失、
  分析師降評;催化改財報優於預期、財測上修、新合約、升評、庫藏股、併購。
  明示輸入英文、輸出繁體中文,並新增標的比對規則對抗 ticker 歧義。
- 時區 UTC+8 → ET:新聞時效 cutoff、prompt 日期與 log 全部改 ET;無時區的
  `published_date` 改視為 ET。
- `LLM_ENRICHMENT_TOTAL_TIMEOUT` 180 → 300s。df_go 上限為
  `TOP_N_RECOMMENDED` = 10 檔,以每檔 ~21s 估算需 ~210s,舊值(台股版 5 檔
  的預算)會讓末尾 1~2 檔靜默落入「整段超時」。
- `LLM_ENRICHMENT_ENABLED` 重新啟用;env `LLM_ENRICHMENT_ENABLED=false`
  快關能力保留,且 Config 關閉優先於 env 開啟。
- `main.py` **未改動** —— 公司名解析放在 `llm_enrichment` 內部,生產掃描
  主路徑零變更。

### 選股中性驗收

- `strategy_config_hash()` 改動前後同為 **`8142e595d788ac06`**;
  `universe_version()` 同為 **`ndx-99-78834e47b659`**。
- `strategy_config_hash()` 是明確 allowlist(`trade_plan.py:217`),新增
  Config 常數不進 hash;`universe_version()` 只 hash `SCAN_POOL`,本次未動。
- 另有測試直接 patch 全部 LLM 設定與 `COMPANY_NAMES`,斷言兩個 identity
  皆不變 —— 防止日後有人把 LLM 設定塞進 cohort identity 而切斷 v1.2.1。
- SignalEngine、TradePlan、ShadowMeasurement、SnapshotSchema 版本均未變更;
  每日 CSV 快照欄位與內容不變。LLM 摘要只寫 Notion「LLM 摘要」欄。

### 測試

- 135 → **165 項全部通過**。新增 `tests/test_llm_enrichment.py` 30 項,
  全程 mock、不打網路,涵蓋 query 美股化、台灣網域缺席、歧義 ticker 公司名、
  未知 ticker fallback、ET 時效過濾(含 naive／UTC published_date)、
  prompt 口徑與日期、Notion 摘要格式不變、金鑰缺失降級、env 快關、
  COMPANY_NAMES 雙向覆蓋與 cohort identity 中性。
- 無金鑰 smoke:兩檔各自降級,回傳 0/2 且不拋例外。
- `git diff --check` 通過。

## 待辦

1. **第一次正式跑後檢查 LLM 摘要品質**:Tavily 命中率、`include_domains`
   是否過窄、Gemini 對英文新聞輸出繁體中文的穩定度。三者只能實跑才知道,
   不佳時調 domains 或 prompt,不需改選股。
2. 下一份週報(週日)會以 V1.3 gate 重寫 `reports/latest.*`;目前 repo 內的
   `latest.md` 仍是 08-02 舊版,帶有已作廢的「D8 門檻已過,可啟動 V1.2.0
   計分核心校準」文案。
3. V1.3 completed-R 累積至 60 筆前,不得調整任何權重、量縮 0.7 或
   `MIN_PRIORITY_FOR_GO`。
4. P9 `backtest_picks` 仍待新引擎樣本足夠後啟動。
