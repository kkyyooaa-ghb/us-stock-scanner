# us-stock-scanner 專案進度存檔(V1.2.1)

存檔時間:2026-07-27
Repo:github.com/kkyyooaa-ghb/us-stock-scanner(public)
Notion:美股掃描 DB `37c323c3fc0180d3a84acdea1a5ca2af`

## 7/26 週報校準判讀

- 週報顯示全期 138 筆、已定案 R 22 筆，但此總數混合兩個策略世代。
- Notion 即時查詢分代結果:
  - V1.2.0：94 筆，已定案 2 筆，R 平均 -1.00，0 勝、2 次停損。
  - 舊 `🔥` 引擎：44 筆，已定案 20 筆，R 平均 -0.263，4 勝、5 次停損。
- 94 + 44 = 138，分代完整，沒有未分類缺口。
- 因 V1.2.0 僅 2/15，D8 尚未通過；不調整量縮 0.7、
  `MIN_PRIORITY_FOR_GO=7` 或計分腿權重。

## 本次修改

- `weekly_report.py` 升級 V1.2.1:
  - 依 `Status` 前綴切分 V1.2.0、舊引擎與未分類。
  - 全期統計保留作 baseline；D8 readiness 只採 V1.2.0 已定案 R 筆數。
  - 週報同時顯示全期與分代樣本、R、勝率及停損率。
  - JSON schema 升為 2，新增 `calibration.engine_stats`。
- 新增 6 項單元測試，涵蓋前綴分代、彙總、Notion rich text 解析、
  未分類排除與 15 筆閘門。
- 分析證據與 notebook 保存於 `.scratch/v12-calibration-gate/`。

## 接續排程

1. 以 `report_week_end=2026-07-26` 重跑週報，修正 `reports/latest.*` 與
   當週歷史快照。
2. 維持現行 V1.2.0 計分核心，繼續由每日掃描與週日回填累積 R。
3. V1.2.0 已定案 R 達 15 筆後，再依策略腿、DistTag 與跳空分組評估調參。

## V1.3 量尺與規則基礎版開始

- 建立 `.scratch/v13-measurement-foundation/` 規格與四張實作議題。
- 新增結構化 `SignalDecision` 與 V1.3 shadow `TradePlan`：
  - 盤整腿區分 MA60 下方 reclaim 與 MA60 上方 limit zone。
  - 超賣腿使用前高 reclaim，避免舊通用進場器輸出 `(0, 0)`。
  - 守均線腿使用實際觸發的 MA20／MA60。
- 每日 CSV 開始保存正式腿別、版本、TradePlan、主題前後分數與市場環境。
- 盤前跳空從 Top 10 擴為完整掃描母體，並修正只寫 DataFrame 副本的問題。
- 舊 LLM enrichment 搜尋台股來源，V1.3 修正前預設停用。
- production Priority、Top 10、Notion 舊進場／停損與績效回填仍維持 V1.2。
- 新增 8 項 TradePlan 與 2 項主題反事實測試；連同既有週報共 16 項通過。

## V1.3 成交與 R 區間量尺

- 新增純函式量尺 `evaluate_trade_plan()`，只接受 immutable TradePlan 與
  as-traded OHLC/actions。
- buy-limit、buy-stop、gap-through、未成交與同日雙觸分開建模；雙觸保存
  R 上下界，不假裝日線能判斷盤中順序。
- 支援 D+20/40/60 總報酬、MFE/MAE、拆股持股倍數與現金股息。
- 新增 `track_shadow_performance.py`，每週從永久 daily snapshots 重算
  `reports/shadow_performance.csv`。
- Notion 舊回填與週報舊 R 明確標記 `legacy-v0`；第一份 forward
  快照前完成盤前硬化後，V1.3 使用 `v1.3.1-shadow`，兩者不混算。
- 全套單元測試增至 28 項並全部通過。

## V1.3 Episode 與完成樣本統計

- 新增 `episode_analysis.py`，以 ticker 交易生命週期把每日重複訊號折疊成
  穩定 `EpisodeId`；持倉中腿別切換不重複開倉。
- 保存 episode 內 signal count、觀察過的腿別／order type 與 canonical
  TradePlan，原始 daily snapshots 不刪除。
- 新增 filled/unfilled/awaiting/open/completed/ambiguous、R 上下界與
  MFE/MAE 統計，並按腿別和 order type 分層。
- 設定全體 completed-R 60 筆最低／100 筆目標、segment 20 筆的調參閘門。
- 每週永久產生 episode CSV、JSON 與 Markdown；閘門不會自動修改權重。
- 全套單元測試增至 36 項並全部通過。

## V1.3.1 Forward snapshot 盤前硬化

- 超賣腿突破錨改為訊號棒高點 `high.iloc[-1]`，不再誤用 T-2 高點。
- 新增 `PlanEarliestEntryDate`：盤前同日可成交；開盤後或休市日補跑從
  下一個 XNYS session 起算；日曆失敗 fail-closed。
- 成交視窗及 D+20/40/60 都從最早可成交日開始；舊量尺版本拒絕混算。
- 新增 `snapshot_schema.py`，正式、空白、錯誤與 workflow fallback
  統一使用 92 欄、原子寫入且可驗證的 CSV contract。
- 休市跳過、資料源失敗與 workflow 缺檔改為 typed control row；週報永久
  歸檔 control，但不讓較晚失敗補跑覆蓋同日成功盤前快照。
- `LegAnchorPrice` 與 ready `PlanAnchorPrice` 使用同一來源，vetoed plan
  不再偽裝成可執行 plan。

## V1.3.2 盤前跳空量尺修正(第一批，選股中性)

2026-07-28 實跑第一份 v1.3.1 快照抽查時發現的資料污染，趕在下一次
09:00 ET 正式掃描前修正。

### 已知無效:PreGap v0(未版本化)

- `get_premarket_quote` 舊版以 Yahoo `regularMarketPreviousClose` 當分母，
  該欄位在盤前時段等於 `Close[-2]` 而非 `Close[-1]`。
- 2026-07-28 實測(93 檔):記錄值與「前一交易日漲跌幅」corr = **0.955**，
  等於把整整一個交易日的移動灌進「跳空」。
- 錯誤分布 −15.82% ~ +12.17%，|gap|≥2% 有 60/92、≥5% 有 26 檔；
  正確分布 −5.2% ~ +2.2%，≥5% 僅 3 檔。13 檔被誤標為「極端跳空」。
- 影響範圍:個股 PreGap、SPY/QQQ 環境欄、L2 大盤燈號與 Notion／TG 操作
  建議。**不影響**個股分數、腿別、Top 10 與 R。
- 2026-07-27 正式快照的 gap 為 v0，**已知無效**，不得進任何跳空統計。
  慣例:`PreGapDefinitionVersion` 空白即 v0 無效，不回寫、不覆蓋。

### 本批修正

- 分母改為「經日期驗證的訊號棒收盤」，與 TradePlan 同源;個股用該列自己的
  `Price`，SPY/QQQ 用 `get_reference_close` 取同基準日線。
- 新增 `preMarketTime` 新鮮度驗證：必須是本日盤前、不晚於掃描時點且
  年齡不超過 60 分鐘，否則為 `stale_quote`;訊號棒非預期最後完整交易日
  → `stale_reference`(當日 ALNY 即由此判出)。
- 新增欄位 `PreMarketPrice`、`PreMarketQuoteTimeET`、`PreGapReferencePrice`、
  `PreGapReferenceDate`、`PreGapReferenceBasis`、`PreGapDefinitionVersion`;
  分母以原值寫入，`gap_pct` 可逐筆重算對帳。
- SPY 參考不合格 → `analyze_market_open` 回 unknown；L2 與 FINAL 都明確
  不提供進場建議(維持 🟡 而非 ⚪，因 FINAL 會忽略 ⚪ 反而可能變綠燈)。
- 訊號棒過期的列 `TradePlanStatus` 降為 `data_stale`，保留全池紀錄但禁止
  進 shadow R;分數、腿別與 Top 10 一律不動。
- `archive_daily_csv` 對較舊 schema 原樣歸檔，升版當週不會中止週報。
- Notion 只寫現行定義下有效的 gap;週報既有跳空分層停用
  (`gap_stratification: disabled_unversioned_pregap_v0`)。
- 調參 cohort 嚴格比對現行 `(SignalEngineVersion, ConfigHash)`；缺欄或
  hash 不符一律排除，報告同步輸出 cohort identity。

### 版本與驗收

- `SnapshotSchemaVersion` v1.3.1 → **v1.3.2**、新增 `PregapDefinitionVersion`
  `v1.3.2-signal-bar-close`;SignalEngine、TradePlan、ShadowMeasurement
  三者**維持不變**。
- 上線驗收(同一訊號棒 2026-07-27 前後對跑):Ticker / Priority / Score /
  SelectedLeg / CandidateLeg / 主題反事實欄 / OrderType / TriggerPrice /
  PlanEntryLow / PlanStopLoss / Top 10 **全部零變動**;僅 PreGap、SPY/QQQ
  環境欄與 schema metadata 改變。
- 單元測試 74 → **90 項**全部通過，新增 `tests/test_premarket_gap.py`、
  L2→FINAL fail-closed、報價年齡／未來時間、exact cohort，以及
  v1.3.1＋v1.3.2 混週歸檔測試。

## V1.2.1／v1.3.3 母體資格與全池對帳(第二批,已完成)

### 為什麼要改

舊門檻 `MIN_AVG_VOLUME_LOTS = 1000`(= 100 萬股/日)是台股「張」的概念。
股數與股價成反比,套到美股後與真實流動性**反向**:靜默排除 6 檔成交金額
3.11 億～11.45 億美元/日的高價股(MPWR 11.45 億高於全池中位數 7.5 億),
卻留下全池唯一低於 1 億的 FER。被排除者完全不進快照 —— 靜默、且有系統
偏誤的 6.1% 資料缺口。

### 門檻依據(執行容量護欄,非報酬最佳化)

99 檔近 20 日每日成交金額中位數:P0 90M / P5 301M / P50 750M / P100 40,773M。
$5M～$50M 全部排除 0 檔。$20M 今天誰也不篩,離全池最低值有 4.5 倍邊際,
作用是在成分股異動或池子擴大時才生效。用中位數而非均值,避免財報日單日
暴量讓一檔矇混過關。

### 切換前 dry run(selection-neutral,未改 repo)

以 monkeypatch 切換門檻,兩次都跑真正的 `run_scanner()`:既有 93 檔的
Priority／Score／ThemeScore／SelectedLeg **零變動**;主題加分 15 → 16 檔
(只有 REGN 新獲得);Top 10 完全相同;shadow_ready 23 → 25。

⚠️ 這是單日結果,**不是每日增量,也不能用來推估閘門會等比例提早**。當天
biotech 本就已有 ≥2 檔觸發,REGN 只是加入已點亮的主題;在 biotech 恰好只有
1 檔觸發的日子,REGN 會讓整池點亮而改變其他股票分數與 Top 10。正式效果仍
需 forward cohort 累積。

### 本批修正

- 流動性改為 20 個完整交易日的 `median(Close × Volume)` ≥ $20M;
  股數門檻完整移除。
- 每檔恰好一筆母體紀錄:可評分者 `data`,其餘 `universe_audit` 附六類原因碼
  (`insufficient_history`／`price_below_min`／`liquidity_below_min`／
  `stale_bar`／`download_missing`／`processing_error`)。不再有靜默 continue。
- `universe_audit` 不得帶 Priority／Score;schema 直接拒絕零分偽裝。
- `expected = processed + excluded + missing` 由 `canonicalize_snapshot`
  永久驗證,重複 ticker 或總數不符即拒寫。
- 訊號棒非最後完整交易日者不參與主題與排名;行事曆不可用時不排除,
  改由量測端 `data_stale` 承接(defense in depth)。
- 新增 `DollarVolumeMedian20` 欄,讓門檻日後可用實際資料重新檢視。

### 版本與驗收

- `SignalEngineVersion` v1.2.0 → **v1.2.1**、`SnapshotSchemaVersion`
  v1.3.2 → **v1.3.3**;TradePlan 與 ShadowMeasurement 維持 `v1.3.1-shadow`。
- ConfigHash `c62717051b17ab85` → **`8142e595d788ac06`**(納入金額門檻、
  回看期、統計方式、最低價與 freshness 規則),v1.2.0 舊 cohort 不會混算。
- 上線驗收:99 = processed 98 + excluded 1(ALNY / `stale_bar`)+ missing 0;
  Top 10 與 baseline 相同;新增 IDXX、REGN 兩筆 TradePlan;
  `DollarVolumeMedian20` 98/98 有值、最低 90M、低於門檻者 0 檔。
- 測試 94 → **105 項**全部通過,新增 `tests/test_universe_audit.py`。

## 第二批原始規劃(已由上節取代)

- 流動性門檻 `MIN_AVG_VOLUME_LOTS = 1000`(= 100 萬股)是台股「張」概念
  沿用，靜默排除 AXON／IDXX／MELI／MPWR／REGN／ROP 六檔;其成交金額為
  3.37 億～12.6 億美元/日。其中 MPWR(ai_semi)、REGN(biotech)、
  MELI(net_consumer)在主題池內，重新納入會改變主題觸發與 Top 10。
- 尚待先跑 99 檔近 20 日 `Close × Volume` 中位數分布再定門檻，
  **不直接採 2,000 萬美元**;門檻是執行容量護欄，不可用報酬最佳化。
- 對帳需涵蓋 `insufficient_history`／`price_below_min`／
  `liquidity_below_min`／`stale_bar`／`download_missing`／`processing_error`，
  並滿足 `expected 99 = processed + excluded + missing`。
- 正式讓 freshness 與成交金額影響主題與排名時才升 `SignalEngineVersion`
  至 v1.2.1，並把價格／流動性／freshness 規則納入 ConfigHash。
- `PreGapStatus` 明確區分 available、無盤前成交、抓取失敗、窗外與停用，
  並輸出全池盤前跳空覆蓋率；calendar 失敗不再阻止盤前報價蒐集，抓取若
  跨開盤則整批作廢。
- 掃描若跨 09:30 ET，依整份快照封存時點統一順延成交日；行情下載是否能
  使用當日 K 則凍結於下載開始時，且要求收盤後 15 分鐘。
- Shadow as-of 改為最後完整 XNYS session；同日盤前尚無 bar 時維持
  `awaiting_fill`。Episode gate 再加三重版本隔離，舊 R 無法解鎖。
- 本地測試 72 項通過；另有 1 項真實 XNYS 休市日測試因隨附 runtime
  未安裝正式 requirements 而條件式跳過。

4. P9 `backtest_picks` 仍待新引擎樣本足夠後啟動。

## V1.2.1 / V1.3.3 review hardening（2026-07-28）

- 修正原實作可能以少於 20 個共同交易日計算成交金額中位數的缺口；
  Close／Volume 視窗不足、日期錯位、非有限值均 fail-closed 為
  `insufficient_history`。
- schema 新增 data 流動性門檻反向驗證，並綁定原因碼與 disposition：
  `download_missing → missing`，其他原因 → `excluded`。
- `UniverseExpectedCount` 禁止小數；使用者可見版本標題改由
  `SignalEngineVersion` 產生。
- 111 項測試通過；1 項真實 XNYS 整合測試因本機缺少
  `exchange_calendars` 而條件跳過。

## 每日快照健康監控（2026-07-29）

- 新增 `snapshot_health.py`，集中驗證 canonical schema、99 檔母體集合、
  Git SHA、快照時點、PreGap 公式／基準日期，以及 Top 10 報價狀態。
- 報告區分 `blocked`、`warning`、`ok`、`skipped`；只有阻擋問題使
  workflow 失敗，單日 excluded／missing／stale quote 保留 shadow
  可用性。
- `scan.yml` 會產出 `snapshot_health.json`、寫入 GitHub Step Summary，
  並將健康報告與原始 CSV 一併保存。
- watchdog 第二次檢查由 09:25 提前為 09:20 ET；兩次檢查為
  09:15／09:20，降低備援掃描跨過開盤的風險。
- 以 2026-07-28 正式 artifact 整合驗證：報告正確為 `warning`、
  `usable_for_shadow=true`，並辨識 ALNY 母體排除、XEL／VRSK
  PreGap 不可用與 XEL Top 10 報價警告。
- 124 項測試通過；1 項 XNYS 整合測試因隨附 runtime 未安裝
  `exchange_calendars` 而條件式跳過。SignalEngine、ConfigHash、
  TradePlan 與 ShadowMeasurement 版本均未變更。
